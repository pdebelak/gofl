!!!
%html
  %head
    %title Game of Life
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    %ul.actions
      %li
        %button#run Run
      %li
        %button#stop Stop
      %li
        %button#reset Reset
    = label_tag :x, "Width"
    = text_field_tag :x, @board.height, id: "x"
    = label_tag :y, "Height"
    = text_field_tag :y, @board.width, id: "y"
    %button#set-board Set Board
    %h4 See an example
    = form_tag find_boards_path do
      = label_tag :slug, "Select example"
      = select_tag :slug, options_for_select(ExampleBoardDecorator.example_options, @board.slug)
      = submit_tag "Select"
    #gofl{ data: { board: { board: @board.board, height: @board.height, width: @board.width }.to_json } }
    -# %h2 Examples
    -# %ul
    -#   - Board.examples.each do |board|
    -#     %li= link_to board.name, board_path(board.slug)
    = yield
